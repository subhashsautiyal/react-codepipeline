version: 0.2

phases:
  pre_build:
    commands:
        # run build script
        - echo Build started on `date` 
        - npm install
       
  build:
    commands:
        # run build script
        - echo Build started on `date` 
        - npm run-script build 
     
artifacts:
  type: zip
  paths:
    - '*'          
