version: 0.2

phases:
  build:
    commands:
        # run build script
        - npm install
       
  build:
    commands:
        # run build script
        - npm run-script build 
     
artifacts:
  type: zip
  paths:
    - '*'          
